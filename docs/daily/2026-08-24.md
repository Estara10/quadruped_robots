# 2026-08-24 Project Daily Report

## 日期

2026-08-24

## 今日目标

- P1-01 — Policy Artifact Provenance and Joint/Contact/Action Order Contract。
- P1-01 本地 I/O contract 验证。
- P1-01F — Deployment Contract Corrections 与 Live Fault Injection。

Day 0 repository/documentation baseline 相关提交也在今天的 Git 历史中，但本日报聚焦 P1-01/P1-01F 的实际工程结果。

## 今日完成工作

### P1-01 初始验证

- 捕获 Isaac Gym Go2 runtime joint/body/feet 顺序，核对 ROS2 → Unitree → MuJoCo motor mapping。
- 验证 controller/policy DOF permutation 的双向映射和 deployed artifact hash、输入输出维度、确定性输出。
- 已确认三个模型均采用原 ABS training implementation；服务器侧 checkpoint、run、export 与 RA dataset/binding 证据仍不可访问，保持 `UNKNOWN`。
- 首次独立 Reviewer 结论为 `REJECT`：provenance/order 未闭环，不能将 conditional remap 提升为 artifact 事实。

### Observation Contract 验证

- 生产链路关联的非对称 golden parity：Agile 61、RA 19、Recovery 49 均逐元素 **0 mismatch**。
- 发现并记录 deployment 与 training 的 Timer、contact temporal filter、Recovery `dof_bias`、ray validity/freshness、NaN/Inf 边界问题。
- Goal shaping 被确认是 `INTENTIONAL ENGINEERING VARIANT`；本日未修改其 gain 或行为。

### P1-01F Deployment Contract Corrections

- 默认 timer 改为显式 rolling time-left，保留命名 legacy mode。
- 部署 contact 使用 training 对应的 current OR previous-policy-cycle filter。
- Recovery observation 使用显式 nominal `dof_bias`，不伪造训练随机 bias。
- 新建 fail-closed ray validity/freshness contract，补齐 observation → RA → action → target → final command 的 finite 检查与 PASSIVE fallback。
- Goal shaping 未改动。

### P1-01F Live Fault Injection

- 第一轮暴露：normal writer false-positive、stop/exit 表观约 4.8s 延迟、无 live command telemetry。后两项的旧延迟来自不同时钟/独立编排，不能作为 freshness 合约证据。
- 将 ray shared memory 升级为 versioned v2 header；writer private calculation 后进行短临界区复制，reader 以 sequence/version 一致快照读取。
- 统一 writer、controller、injection、detection 与 command telemetry 为 `steady_clock` ns。
- 新增 simulation-only internal fault injection；real launch 强制禁用。新增最终 command、Kp/Kd/torque、finite/veto telemetry。
- 最终 live result：normal writer PASS；writer stop 211.706ms PASS；writer exit 205.353ms PASS；ray NaN/Inf、observation NaN、RA NaN、action NaN、joint target NaN、final motor command NaN/Inf 均 PASS。
- 每个 fail-closed case 均证明最终 motor command finite、Kp/Kd/torque 归零、RL 不再继续控制；P1-01F live contract 已 PASS。

## 今日关键技术结论

1. Agile 61、RA 19、Recovery 49 的布局和逐元素构造已在本地验证通过。
2. Isaac Gym 与 ROS2/Unitree/MuJoCo joint order 不同，但当前 DOF permutation 和 motor mapping 对已捕获 training order 正确。
3. Timer、contact filter、Recovery `dof_bias`、ray validity 和 non-finite runtime contract 的已知部署问题已修复并经 helper/live 测试验证。
4. Ray freshness 的协议时序问题已定位并修复。当前 live 200ms freshness contract 在 ROS2+MuJoCo 中成立，工程容差为一个实测 20ms ray-check 周期。
5. Goal shaping 是工程增强 variant，不能混入 paper-faithful 结论。
6. P1-01 整体仍不能 PASS：server-side model provenance/order 证据未恢复。

## 今日测试与证据

- `colcon build --packages-select rl_quadruped_controller --cmake-args -DBUILD_TESTING=ON`：PASS。
- `cmake --build build2 --parallel 2`（MuJoCo）：PASS。
- Agile 61 / RA 19 / Recovery 49 golden parity：均 0 mismatch。
- P1-01 contract regression：135 PASS / 11 KNOWN / 0 FAIL。
- Live ROS2+MuJoCo 10 类 fault injection：PASS；当前 scope 内无 P1-01F FAIL。
- Measured writer period：1.0022ms mean；measured ray-check period：20.0001ms mean。

证据入口：[`p1_01f_local_contract.json`](../evidence/P1-01/p1_01f_local_contract.json)、[`POLICY_IO_CONTRACT.md`](../POLICY_IO_CONTRACT.md)、[`REVIEW.md`](../evidence/P1-01/REVIEW.md)。

## 当前项目状态

### P1-01

`BLOCKED / PARTIALLY COMPLETE`：server-side artifact provenance/order 尚未闭环。

### P1-01F

`PASS`

### Phase 1

`NOT ACCEPTED`

### Phase 2

`NO-GO`

### Real Robot ABS/RL

`NO-GO`

## 当前剩余问题

- Agile/RA/Recovery server-side checkpoint/export/run provenance。
- RA dataset 与 Agile binding 证据。
- actual deployed artifact order 的 provenance-backed 证明。
- real Go2 `foot_force[0..3]` 语义。
- Goal shaping 仍为 engineering variant，后续需分离 paper-faithful/stabilized。
- 当前 `CURRENT_STATE.md` 记录的 termination-contact、Recovery solver/switching、formal experiment validity/seed/event recording 等 Critical/High blockers。

## 今日 Git 总结

- Branch：`feat/ray-pred-source-switch`。
- End-of-day commit：`test: close P1-01F live fault contract`；其前一 HEAD 为 `8d0ef43` (`fix: enforce ABS deployment input contract`)。
- 今日关键 commits：`7ad7a9e` P1-01 contract evidence、`539b8af` local observation parity、`8d0ef43` deployment input contract，以及 P1-01F live fault contract 收尾提交；Day 0 baseline commits 亦在今日历史中。
- 相对 `main`：ahead 15 / behind 0。
- End-of-day 工作树不 clean：仅保留已知用户修改 `paper.txt` 删除、path gain、real-network 注释和未跟踪 legacy report generator。P1-01F source/doc/evidence 已提交；未删除或覆盖未知内容。

## 明日计划

1. 先检查训练服务器是否恢复可访问。
2. 若可访问：继续 P1-01 provenance/order closure，找回 Agile/RA/Recovery checkpoint、run metadata、export 关系和 RA dataset binding；完成后执行独立 Reviewer 复审。
3. 若仍不可访问：P1-01 保持 BLOCKED，开始 P1-02 Formal Experiment Contract；不得重复本地 P1-01F 工作。

正式开始前以 [`CURRENT_STATE.md`](../CURRENT_STATE.md) 为准。

## End-of-Day 检查

- `CURRENT_STATE.md` 正确记录 Phase 1、P1-01 BLOCKED、P1-01F PASS、server provenance blocker、Phase 2/real ABS-RL NO-GO。
- 未将 P1-01 标为 PASS，未将 P1-01F 标为 FAIL。
- 未启动 P1-02，未运行真机，未处理新的非阻塞算法问题。
