# 项目日报 — 2026-08-28

## 今日目标

推进 Phase 1 的 P1-09：建立真实运行数据可观测链路，并关闭 MuJoCo 与
StateRLRec 的受控退出稳定性风险。

## 今日完成

- P1-09C/D 的实时帧与终端 HUD 经 fail-closed 修复和独立审查后，完成一次
  严格限界的 MuJoCo-only HUD 真实性验证：HUD 仅显示共享内存中的真实帧，
  控制器退出后帧立即失效，无残留数值显示。
- P1-09F 修复 StateRLRec 未 join 的后台线程；离线生命周期测试、控制器构建
  和独立审查均通过。
- P1-09G/I 在真实仿真中观察到 Recovery enter/exit、controller/plugin 正常
  关闭与 ROS launch `rc=0`；但 MuJoCo 对 SIGINT 未退出、随后被 SIGTERM
  结束，`rc=143`。该事实已纠正并保留为 FAIL，不被写成 clean shutdown。
- P1-09J/K 静态与最小运行诊断确认：MuJoCo 运行时忽略 SIGINT
  （`SigIgn=0x6`）；UI-close 全进程行为仍为 UNKNOWN。
- P1-09L 完成 graceful-shutdown 设计并获条件性审查。
- P1-09N 恢复 SDK 线程契约：SDK `RecurrentThread` 是 detached pthread，
  析构取消但不 join，不能作为 m/d 生命周期安全边界。
- P1-09O 用项目自有可 join 的 RobotBridge 线程替代 SDK detached 回调；
  离线构建和生命周期测试通过。

## 当前状态

- Phase：Phase 1 — MuJoCo Simulation Validation，**NOT ACCEPTED**。
- P1-01：BLOCKED / PARTIALLY COMPLETE；历史 order、RA binding 等 UNKNOWN。
- P1-02、P1-03：仅离线范围 ACCEPTED。
- P1-09：EXECUTING / NOT ACCEPTED。
- Phase 2：NO-GO。

## 今日发现的关键阻塞

P1-09O 独立 Reviewer 发现 model reload 竞态：PhysicsLoop 可替换
`mjModel/mjData`，但 RobotBridge 仍可能持有旧指针。即使正常退出路径正确，
reload 时仍可能 use-after-free。另有 JoinableThread 并发 start/stop 和外部
DDS 线程退出语义未闭合。

## 明日待决事项

等待 Director 确认 fail-closed reload 安全屏障：bridge 活跃时不得直接 reload
`m/d`；必须先 stop + join 所有 m/d-accessing worker。未确认前，不实施后续
生命周期修复，也不再运行 MuJoCo clean-shutdown 验证。

## 明日计划（确认后）

实施最小 reload 安全屏障、修复 JoinableThread 并发窗口、审计 DDS 线程边界；
完成独立审查后，再授权一次 MuJoCo-only clean-shutdown 验证。
