# 2026-08-30 日报

## 今日结论

- **P1-09 已 ACCEPTED**：一次受控的 simulation-only 运行证明了
  `MuJoCo + StateRL → HUD → authoritative runtime JSONL → two-phase finalize
  → actual process facts → post-run summary → P1-02 validator` 主链。P1-02
  validator 对该 episode 的结论为 `INVALID`，原因是 simulation time、goal、
  timeout、collision 和 fall 缺少权威来源；没有伪造 `SUCCESS` 或 `VALID`。
- **P1-01 已 ACCEPTED WITH KNOWN ISSUES**：独立 Reviewer 接受 DEC-010
  修订后的边界。Agile、RA 与 Recovery 部署权重 lineage 已确认；声明式
  `FL,FR,RL,RR → remap → FR,FL,RR,RL` 操作映射受既有非对称 contract
  证据支持。
- `ABS_fuwuqi/ABS` 已登记为恢复的训练服务器快照证据根目录。今后检索训练
  checkpoint、TensorBoard、导出模型、RA 或 Git 历史时，必须同时检查它和
  canonical `ABS/` 根目录。

## 保留边界

- P1-01：历史 config/seed/command/Git/export/raw RA dataset 为 deferred
  reproducibility；RA 使用 `model_4000.pt` 是 `OPERATOR_DECLARED`；真实 Go2
  `foot_force[0..3]` 槽位语义为 Phase 2 hardware-only。
- P1-09：P1-09AE runtime-record 子链为 ACCEPT WITH KNOWN ISSUES。真实记录
  主链已成立，但本次 formal verdict 是诚实的 `INVALID`，不是 benchmark、
  性能或真实机器人结论。
- Phase 1 仍为 **NOT ACCEPTED**；Phase 2 ABS/RL 仍为 **NO-GO**。

## 验证

- P1-01 contract validator（`abs` conda 环境）：`pass=135`、`known=8`、
  `fail=0`，`CONTRACT REGRESSION: PASS`。脚本退出码 `2` 来自其 pre-DEC-010
  的旧 Acceptance 摘要，不是 contract regression。
- P1-01 与 P1-09 的 Reviewer 结论、运行证据和状态文档均已归档。

## 明日候选

- 当前没有活跃工程任务。
- 建议下一项为 **P1-04 — Agile 61-D observation parity**：在不调参、不重训、
  不改策略的前提下，正式比较训练侧 `compute_observations()` 与部署侧
  `StateRL` 的 61 个字段、顺序、单位、缩放与有效性规则。

## Git / 推送状态

- **未推送远端。** 工作区含大量混合的已修改与未跟踪内容，包括 P1-09
  代码/证据、`ABS_fuwuqi/` 服务器快照、既有用户改动及 `paper.txt` 删除。
- 在按任务/证据边界分组、逐项审核并形成明确 commit 前，不应整体提交或
  推送到 `origin/feat/ray-pred-source-switch`。
