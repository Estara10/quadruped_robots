# P1-08 — Baseline Capture BLOCKED (2026-09-01)

Status: **BLOCKED — runtime capture FAIL (single authorized run consumed on a
stale MuJoCo binary); corrected binary built and verified offline; a new run
requires Director re-authorization. No retry was performed (task rule).**

## Summary

The P1-08 single authorized simulation-only capture was attempted once at
13:17:21 on 2026-09-01 and **failed before sampling**. The orchestrator
preflight passed; MuJoCo started, loaded `scene_flat.xml` (MuJoCo 3.3.3,
model/shms initialized, ray source geometric), but the observability physics
clock (`/mujoco_sim_clock`) was **never created** and never advanced, so the
orchestrator failed the run at `wait_sim_clock_advance` and sent SIGINT (no
TERM/KILL). Per the task rule ("if preflight or run fails, stop and record
BLOCKED/FAIL — no retry"), **no second run was attempted**.

## Root cause (identified, launch-pipeline defect)

The MuJoCo executable that ran was **stale (pre-instrumentation)**:

- `unitree_mujoco/simulate/build2/unitree_mujoco` mtime = **2026-08-29 14:24**;
  `src/main.cc` (with the P1-08 sim-clock edits) mtime = **2026-09-01 13:11**.
- The first `make p1_08_sim_clock_test p1_08_model_probe unitree_mujoco`
  invocation **aborted (exit 2)** at a `p1_08_model_probe.cpp` compile error
  **before relinking `unitree_mujoco`**. The second invocation only rebuilt the
  probe. The stale 08-29 binary therefore contained **no** sim-clock code.
- `nm -C build2/unitree_mujoco` (before rebuild) showed no
  `abs_sim_clock::SimClockWriter` symbol; after the completed rebuild it is
  present.
- Orchestrator preflight checked binary existence/executability but **not
  binary freshness** — this is the defect to fix before any future run.

`sim.run = 0` at `main.cc:291` is only the model-load-warning path and is **not**
the cause (the log shows a clean load with no warning).

## Evidence (preserved raw)

- `docs/evidence/P1-08/capture_20260901/orchestrator_raw.log` (preflight PASS →
  MuJoCo pid 39319 → 15 s wait → FAIL)
- `docs/evidence/P1-08/capture_20260901/mujoco_raw.log` (MuJoCo 3.3.3 init,
  model/shms, ray source geometric; no physics stepping output)
- `/dev/shm/mujoco_sim_clock` was NOT created by the stale binary (verified
  post-run). No lingering MuJoCo process remained after the orchestrator's
  SIGINT + wait.

## Offline deliverables that COMPLETED (hash-bound, no runtime)

These are valid regardless of the failed run:

- **Part A manifest** `P1-08_baseline_manifest.json` (schema v1):
  - model closure sha256 `8d9218de…` over `scene_flat.xml` + `go2.xml` + 18
    present files, **no missing asset**.
  - binaries: `mujoco_executable` sha256 `298d9b70…` (**corrected** build),
    `libmujoco` `e35ba7f6…` (runtime-resolved path hash-equal), controller
    plugin `2b31e558…`, hardware plugin `9c56d00d…`.
  - deployed artifacts: Agile `5a87d692…` (801 726 B), RA `05c40ff7…`
    (32 011 B), Recovery `e3047a21…` (775 715 B) — **consistent with P1-01**.
  - git commit `d27f34d` branch `feat/ray-pred-source-switch` **dirty=True**.
  - effective static model facts (probe): MuJoCo 3.3.3, `opt.timestep=0.002`,
    Euler, elliptic cone, Newton solver, iterations 100, tolerance 1e-8,
    gravity (0,0,−9.81), impratio 100; nq19/nv18/nu12/njnt13/nbody18/ngeom57/
    nsite5/nsensor45/nmesh16; actuator ctrlrange ±23.7 (hip/thigh) / ±45.43
    (knee); joint ranges; 10-step advance = 0.02 s (confirmed).
  - static controller facts: controller_manager 1000 Hz, rl controller
    `update_rate=200`, `decimation=4`, `model_folder=abs`, `use_rl_thread=false`,
    `switching_mode=stabilized_switch` (P1-07 default), `ra_threshold=-0.05`,
    hold 30; `mujoco_simulate.robot_scene=scene_flat.xml`; duplicate
    `domain_id`/`interface` keys recorded as **static ambiguity**.
- **Part B observability instrumentation** (proven behavior-neutral offline):
  - `common/abs_sim_clock_contract.h` (new; steady_clock, 40-byte struct,
    single-writer shm); `main.cc` publishes after each `mj_step` (two sites);
  - mechanical test `p1_08_sim_clock_test` **PASS** (writer→reader round-trip);
  - model probe `p1_08_model_probe` **exit 0**.
- **Authoritative timing source reuse** confirmed: `/mujoco_rt_frame`
  (StateRL `writeRtFrame` once per RL step, `decimation=4` gating in `run()`)
  measures the policy/RL-step period; RA ticks per RL step (source-verified);
  Recovery ticks from `policy_state`; individual 200 Hz controller callbacks are
  **not exposed** by any authoritative source (direct observation UNKNOWN).

## What was NOT produced

- No runtime timing capture (no samples); observed physics/policy/RA/Recovery
  periods and the baseline freeze artifact were **not** produced — they cannot
  be without a valid run.
- `P1-08_simulation_baseline.md/.json` freeze artifact: **not written**
  (requires a successful capture).

## Corrected state and required re-authorization

- `unitree_mujoco` rebuilt (mtime 2026-09-01 13:19) with the sim-clock
  instrumentation; `SimClockWriter` symbol present (offline-verified); build
  **exit 0**.
- Orchestrator preflight must be hardened (binary-freshness check) before any
  future run.
- A second run is **not** authorized by this record; it requires explicit
  Director authorization (analogous to the P1-09AE env-correction grant).

Phase 1 remains **NOT ACCEPTED**. No P1-10 work was started.
