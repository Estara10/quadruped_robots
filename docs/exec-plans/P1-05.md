# P1-05 — RA Label, Model Semantics, and Agile Operational Binding

Status: **ACCEPT WITH KNOWN ISSUES — independent review 2026-08-31**. No active
engineering task after closure; Phase 1 remains NOT ACCEPTED; P1-06/P1-07 must
not start automatically. Known issues retained: no label numeric fixture
(Bellman MATCH is structural/source-level, not end-to-end numerical);
paper Eq.18 exact log base / σ / terminal bootstrap / loss scale UNKNOWN;
RA↔Agile binding OPERATOR_DECLARED under DEC-010; the Reviewer-corrected
recovered-testbed switching fact is `recovery = (v_pred > -twist_eps = -0.05)`
(strict `>`, immediate, no `-0.08` exit, no 30-step hold). See
[`REVIEW_2026-08-31_RA_SEMANTICS.md`](../evidence/P1-05/REVIEW_2026-08-31_RA_SEMANTICS.md).

## Objective

Establish or precisely classify the semantic chain:

```
recovered RA training-label construction
  → deployed RA 19-D input
  → deployed ra_value.pt output meaning/range
  → StateRL switching comparison / Recovery entry
```

Mode: offline evidence audit only. No MuJoCo/ROS2/training/export/benchmark/
hardware. No production code/config/model/threshold/state-machine change.

## Scope

1. Trace the recovered RA label from the server snapshot
   `ABS_fuwuqi/ABS/training/legged_gym/legged_gym/scripts/testbed.py`; write the
   exact hand-computable expressions; compare to paper Eq.16–18
   (ABS_PAPER_NOTES).
2. Slot-by-slot 19-D deployed RA input (source/frame/unit/scale/order/transform,
   goal shaping, ray freshness/finite boundary) from `StateRL` to
   `runRAModel()`/`ra_value.pt`.
3. Deployed RA: `ra_value.pt` shape/range/direction; every `StateRL` comparison
   (operator, threshold, hysteresis, forced hold); paper vs stabilized
   switching difference.
4. DEC-010 boundary: RA ↔ Agile `model_4000.pt` is **OPERATOR_DECLARED**
   operational binding only; historical seed/command/Git/dataset are deferred,
   not blockers.

## Required evidence roots searched

- `ABS/` (working-tree legged-gym `testbed.py`, `go2_pos_config.py`)
- `ABS_fuwuqi/ABS/` (server snapshot `testbed.py` — the recovered RA training
  label code; the two roots are byte-identical for `testbed.py` in the audited
  lines)
- `quadruped_ros2_control_humble/.../FSM/StateRL.cpp`,
  `.../FSM/AbsObservationContract.cpp`
- `docs/evidence/P1-01/` (RA weight chain), `docs/ABS_PAPER_NOTES.md`,
  `docs/POLICY_IO_CONTRACT.md`, `docs/evidence/P1-03/formula_parameter_trace.yaml`

## Deliverables

- P1-05 evidence report (`docs/evidence/P1-05/P1-05_ra_semantics_20260830.md`)
- machine-readable 19-D matrix
  (`docs/evidence/P1-05/P1-05_ra_19d_matrix_20260830.json`)
- paper / recovered-training / deployed three-way comparison
- every difference classified MATCH / INTENTIONAL_VARIANT / MISMATCH / UNKNOWN
- `git diff --check`
- status `IMPLEMENTED / AWAITING INDEPENDENT REVIEW`

## Stop conditions

If a label/binding evidence item is absent after the search, record the exact
searched boundary and keep UNKNOWN — never back-infer from the RA output.
If an inconsistency is found, record only; do not fix. If runtime or a behavior
change is required, stop and report.
