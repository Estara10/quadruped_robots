# P1-06 — Recovery Eq.21/Eq.22 and Safe-Twist Optimizer Parity

Status: **IMPLEMENTED / AWAITING INDEPENDENT REVIEW** (not self-accepted).
Independent review 2026-08-31 returned **REJECT** for a classification
overclaim (paper parity, testbed↔deployment parity, and paper-unspecified items
were conflated); the evidence was corrected to two-dimensional verdicts and
paper-undefined items are now paper `UNKNOWN`. See
[`REVIEW_2026-08-31_OPTIMIZER_PARITY.md`](../evidence/P1-06/REVIEW_2026-08-31_OPTIMIZER_PARITY.md).

## Objective

Compare paper Eq.21/Eq.22 → recovered ABS training/reference testbed optimizer
→ current `StateRL` deployment optimizer, and classify each Recovery safe-twist
aspect (objective, RA constraint, gradient iteration, clamp, predicted
displacement, RA-model input) as MATCH / INTENTIONAL_VARIANT / MISMATCH /
UNKNOWN — with every Eq.21 verdict two-dimensional (`paper relationship` and
`testbed ↔ deployment relationship`).

## Deliverables

- `docs/evidence/P1-06/P1-06_recovery_optimizer_parity.md` (three-way matrices)
- `docs/evidence/P1-06/P1-06_recovery_optimizer_matrix.json` (machine-readable)
- `scripts/test_p1_06_recovery_optimizer.py` (deterministic numeric fixture:
  Eq.22 yaw-coupling, Eq.21 objective arithmetic, clamp boundary, gradient-clip
  difference)

## Boundaries

- No change to `StateRL`, Recovery/RA model, thresholds, hold, gains, solver,
  scene, dynamics, controller, or config.
- No MuJoCo/ROS2/training/export/benchmark/formal/hardware; no P1-07/P1-08.
- No re-tracing of DEC-010-deferred historical seed/command/dataset/Git.
- A discovered MISMATCH is recorded, not auto-fixed.

## Key findings

- Eq.22: reference `get_pos_integral` (`testbed.py:55-61`) matches paper
  (yaw-coupled second-order terms); deployment `pos_x=vx*tau` / `pos_y=vy*tau`
  (`StateRL.cpp:623-624`) omits them → **MISMATCH** (recorded gap,
  ABS_PAPER_NOTES:131).
- Eq.21 (two-dimensional; paper relationship vs testbed↔deployment
  relationship):
  - Objective constants (lam=10, eps=0.05, goal coeff 0.02): paper **UNKNOWN**
    (paper does not give numeric constants); testbed↔deployment **MATCH**.
  - RA penalty (λ/ε): paper **UNKNOWN**; testbed↔deployment **MATCH**.
  - Learning rate: paper **UNKNOWN**; testbed↔deployment **MATCH**.
  - Candidate twist → 19-D RA input: paper **UNKNOWN** (paper lacks the full
    concatenation); testbed↔deployment **MATCH**.
  - Initialization: paper **MATCH** (paper: "from the current twist",
    ABS_PAPER_NOTES:120); testbed↔deployment **MATCH**.
  - Twist bounds ±[1.5,0.3,3.0]: paper **MATCH** (ABS_PAPER_NOTES:108-114);
    testbed↔deployment **MATCH**.
  - Output clamp type / feasibility check / infeasible fallback: paper
    **UNKNOWN**; testbed↔deployment **MATCH** only for the observed behavior
    (component-wise clamp; both absent) — this is **NOT** paper MATCH.
  - Gradient clip: paper **UNKNOWN**; testbed↔deployment **MISMATCH**
    (L2-norm vs per-element; not an approved stabilized variant).
  - Iterations: paper ≤5 (ABS_PAPER_NOTES:121); testbed 10 **not paper MATCH**;
    deployment 3 **upper-bound MATCH only**; testbed↔deployment **MISMATCH**.
  All recorded, not fixed.
- Numeric fixture 14/14 PASS proves the Eq.22 yaw-coupling difference and the
  grad-clip difference.

P1-06 remains **IMPLEMENTED / AWAITING INDEPENDENT REVIEW**; Phase 1 NOT ACCEPTED.
