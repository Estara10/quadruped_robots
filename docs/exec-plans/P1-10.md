# P1-10 — Deterministic Scenario Suite and Root-Seed Propagation

Status: **P1-10 — IMPLEMENTED / AWAITING INDEPENDENT REVIEW**

This increment closes the P1-10 Reviewer findings on runtime-bound initial state
and variant truthfulness. The implementation and offline validation remain
complete, but the separately authorized same-seed replay pair
`P1-10-REPLAY-20260902-flat_goal_forward-stabilized` was attempted once and
failed at preflight before any child launch. Run B was not attempted and no
retry was made.

## Boundaries

- P1-08 accepted v2 baseline is read-only input; its evidence and identity are
  not rewritten.
- No policy, RA, Recovery, P1-07 switching semantics, scene dynamics, model,
  PD, solver, timestep, or gains changes were made.
- The five historical obstacle roots are offline candidates. The canonical
  `obstacle_test1` collision source contract is implemented and independently
  accepted with known issues, but no obstacle runtime validation exists; all
  candidates remain outside the accepted capture suite.
- Phase 1 remains **NOT ACCEPTED**. P1-11, P1-12, and P1-13 were not started.

## Behavioral-validation gate — DEC-011

P1-10 final acceptance is not established by flat replay alone. The Director-
approved behavioral-validation stages are:

### Stage A — Flat replay

Run the flat replay to validate the minimum infrastructure, repeatability, and
saved-record comparison chain. A Stage A pass is a sub-gate only; it is not an
algorithm-validity proof, P1-10 final acceptance, or authorization for P1-11.

### Stage B — First obstacle map behavioral run

Freeze the first obstacle map and perform one real behavioral run. Confirm that
the following data have real runtime sources and are explainable:

- ray;
- RA;
- Agile/Recovery switching;
- collision;
- terminal/outcome;
- runtime record.

The obstacle definition and authority, collision/terminal authority, runtime
record provenance, and repeatability must all be closed before this stage can
support final acceptance. The final number of obstacle maps is not fixed by
this plan.

### Stage C — Formal scenario-suite expansion

Expand the remaining obstacle scenarios and form the formal scenario suite.
Only after the approved P1-10 multi-obstacle suite is accepted may the roadmap
sequence proceed to P1-11 pilot and then P1-12 multi-seed evaluation. No later
task starts automatically.

## Hash-bound suite

Suite manifest: `scenarios/p1_10/scenario_suite_manifest.json` — SHA-256
`eb81d60742864fe9c870e957ba3ab601e80da3e64bc48a42c26f849570f3152d`.

| Scenario | Status | Scenario file SHA-256 | Scene root SHA-256 | Model closure SHA-256 | Goal | Obstacles |
|---|---|---|---|---|---|---|
| `flat_goal_forward` | `SUPPORTED` | `beba99ed4e6f6c8f84eb1ac514f2da4b6e910c1587fdf91f5e95ac6bc639e092` | `9ce83b3e61c722a523d0359536cee803f17610f95d2275fc32e96801ec3c1908` | `8d9218de0dc02978fc0ef4ba1c790fa3b968fbdbfdb945e14522436a2574ea07` | `[7.0, 0.0]` world m | explicit `EMPTY` |
| `flat_goal_lateral` | `SUPPORTED` | `c95f83c345797ce31a449e27df16cffe84f3650ed2be498ac79cef51f6e09ff5` | same | same | `[7.0, 1.5]` world m | explicit `EMPTY` |
| `static_obstacle_authority_unavailable` | `UNSUPPORTED` | `60c217fd616818637ec0e3435ba3144ce9f01246d4429a16011af8969e746f53` | same flat root (not capture-eligible) | same | fixed `[7.0, 0.0]` | explicit `UNSUPPORTED`, objects `[]` |

Both supported scenarios bind `initial_state_source.kind=scene_default` to the
actual `PhysicsThread` construction path
`mj_loadXML -> mj_makeData -> sim.Load -> mj_forward`; no `home` keyframe reset
is performed. The offline probe extracts the exact 19-element `qpos0`, whose
SHA-256 is `a604dd11dc57ea655bf6d746dcf068a91e80a0a1eddc73d20c1a3800468f59d8`.
It derives base pose `[0, 0, 0.445]` m and yaw `0` from that qpos, and binds the
probe source hash plus scene closure hash. Manual pose/yaw/source/hash changes
therefore fail preflight. Both also declare fixed switching mode
`stabilized_switch`, `run_window_s=25.0`, fixed world-frame goal,
`resample_goal_on_arrival=false`, and `config_overrides={}`. Because the current controller applies joystick trim to
its configured world goal, `goal_injection` is also explicit: forward uses
`lx=ly=rx=0`, while lateral uses `lx=-0.75` to produce exactly `[7.0,1.5]`
m from the accepted baseline `[7.0,0.0]` m. The harness sends that declared
input before RL; it is not a silent config default. The accepted P1-08 v2 baseline identity is
`59dd13fed5ebd026ec519f2659643237502be8e4d8df5174a65b7d35ceb4f7e0`; the
baseline manifest SHA-256 is
`2667ed37a854f85e5a7c493e7d4a8b1871a84ce95d3e3b0742801d383f8dc915`.

## Supported-variant closure

| Label | Actual consumed path | Binding hash | Verdict |
|---|---|---|---|
| `stabilized` | `abs/config.yaml` with `switching_mode=stabilized_switch`, consumed by deployed `rl_quadruped_controller` | `2f0dfc4e8bf5237a578d99030facc38459fd5f899af49b508e48e29b7e8a4e1c` | `SUPPORTED` |
| `paper-faithful` | no P1-10 capture binding to a consumed configuration | n/a | `UNSUPPORTED`, preflight reject |
| `agile-only` | no Agile-only runtime consumer | n/a | `UNSUPPORTED`, preflight reject |

The stabilized binding includes the accepted ABS config hash
`1cd42c4bb29baad1873bd55e7f2f1d82fb0c8ec8bf35fb8e4eff61338758c586` and
deployed controller plugin hash
`2b31e558471227a385906239a4fd20d1f9cb759a4960c743b6f5065f13fe6d4e`.

## Root-seed registry

Derivation is exactly the accepted P1-02 rule:
`SHA256(f"{root_seed}:{declared_label}")[:8]` interpreted big-endian and
masked to `0x7fffffff`.

| Source | Flat scenario disposition | Actual injection/evidence |
|---|---|---|
| scenario initial-condition sampling | `NOT_USED_IN_THIS_SCENARIO` | actual `mj_makeData:qpos0` probe binding; no sampler |
| MuJoCo/model randomization | `NOT_USED_IN_THIS_SCENARIO` | fixed accepted P1-08 closure; no randomization entry point |
| controller/goal resampling | `NOT_USED_IN_THIS_SCENARIO` | `resample_goal_on_arrival=false`; C++ `rand()` branch unreachable |
| Python orchestrator | `DECLARED_NOT_CONSUMED` | no Python random consumer in current capture path; root seed is pairing/provenance only |
| C/C++ `rand` | `NOT_USED_IN_THIS_SCENARIO` | StateRL resample branch is disabled; no fixed default is substituted |
| Torch/Python/NumPy sampling | `NOT_USED_IN_THIS_SCENARIO` | current capture path performs inference/recording only; no sampling producer |
| obstacle placement | supported: `NOT_USED_IN_THIS_SCENARIO`; obstacle slot: `UNSUPPORTED` | supported scenarios declare `EMPTY`; unsupported obstacle slot cannot launch |

The resolved manifest records the root seed as pairing/provenance identity,
every registry disposition, scenario hash, suite hash, baseline identity,
runtime initial-state binding, consumed variant binding, and exact launch
contract. The current `derived_seeds` map is empty; P1-02 derivation remains
available only when a real producer and consumer are added. A future source
that cannot be injected must be marked `UNSUPPORTED` and is rejected before
capture.

## Harness enforcement

`scripts/p1_08_baseline_capture.py` now requires `--scenario`, `--root-seed`,
`--variant`, and `--manifest`. Before any runtime prerequisite or child launch
it resolves and verifies the scenario, suite index, accepted P1-08 manifest,
identity, full model closure, actual initial-state probe/source, scene
argument, explicit initial-state source, fixed window, root-seed registry, and
consumed variant binding. A successful preflight writes
`scenario_resolved_manifest.json` and `p1_10_context.json` only into that new
capture directory and records the same formal/process context in process
facts. Unsupported variants and initial-state-source mismatches fail before
any runtime prerequisite or child launch. Residual-process detection uses a
`/proc` identity snapshot: exact MuJoCo executable identity, attributable
`ros2 launch rl_quadruped_controller mujoco.launch.py`, and
`ros2_control_node` with the Go2 controller config are rejected; self/ancestor
PIDs and shell/path-only mentions are excluded; inspection failure or
ambiguous controller attribution returns `uncertain` and is rejected.
Existing P1-08 capture evidence was not modified.

## Offline verification

| Check | Result |
|---|---|
| P1-10 scenario/seed/initial-state/variant/preflight tests | **12/12 PASS** |
| P1-02 formal validator/seed/pairing regression | **22/22 PASS** |
| P1-08 harness regression, including residual-process identity tests | **93 checks PASS** |
| suite index, scenario files, baseline identity and closure hashes | **PASS** |
| same scenario + root seed byte-stability | **PASS** |
| changed root seed changes pairing identity; no runtime random consumer claimed | **PASS** |
| actual MuJoCo construction-path qpos/pose/yaw binding | **PASS** |
| supported/unsupported variant closure and label-only mutation rejection | **PASS** |
| field/scene/override/registry hash sensitivity | **PASS** |
| missing field/path escape/unsupported source/resample rejection | **PASS** |
| launch argument mismatch and cross-pair rejection | **PASS** |

Commands used were offline Python tests, the no-`mj_step` model-construction
probe, and static/hash validation only. No same-seed runtime replay was
demonstrated. The Director-authorized replay attempt failed before runtime
because the broad residual-process `pgrep` matched the current Codex sandbox
command line containing the MuJoCo path. Its raw evidence is archived under
`docs/evidence/P1-10/replay_pair_20260902/`; Run B was not attempted and no
retry was made. Runtime event-sequence replay and runtime producer coverage
remain `UNKNOWN`/deferred to a new Director decision.

## Historical Director-authorized same-seed replay attempt

The pair manifest was frozen before Run A. It binds `flat_goal_forward`, root
seed `20260902`, `stabilized`, `stabilized_switch`, the accepted P1-08 v2
manifest/identity, the resolved manifest, initial-state binding, and the exact
canonical comparison protocol. Pair manifest SHA-256 is
`a5d5de6c58629121a4316719f1fb9ed10d1a3e2e221c40ce57b1236af89fb080`.

Run A was attempted exactly once. Scenario context resolution passed, then the
residual-process guard failed because `pgrep` matched the active Codex sandbox
command line (the command line contained `unitree_mujoco`). No MuJoCo/ROS2
child was launched; therefore no PID/PGID, signal, wait, runtime record,
timing record, or canonical projection exists. Run B was not attempted, and
the pair is `FAILED_FOR_THIS_PAIR` without retry. See
`docs/evidence/P1-10/replay_pair_20260902/pair_result.json`.

## Historical same-seed replay pair — 2026-09-03

The new pair `P1-10-REPLAY-20260903-flat_goal_forward-stabilized` was frozen
before Run A with pair-manifest SHA-256
`b86a19887dee8a441c7a5643eca698ea4a092a92bd549e972d41b1067c8f049e` and
resolved-manifest SHA-256
`9b67bbd30dd363cc035a9dc3896e195027fb18ae8170f36b60648b47073cea42`.
Run A was attempted exactly once. Residual-process identity preflight passed
with no matches, then X11 preflight failed with `xdpyinfo rc=1`; no ldd or
runtime child was launched. Run B was not attempted and no retry was made.
The pair is `FAILED_FOR_THIS_PAIR`; no runtime record, timing record, process
facts, raw logs, or canonical replay comparison exists. Evidence is archived
under [`replay_pair_20260903`](../evidence/P1-10/replay_pair_20260903/), with
machine-readable result in
[`pair_result.json`](../evidence/P1-10/replay_pair_20260903/pair_result.json).

The original `P1-10-REPLAY-20260902-flat_goal_forward-stabilized` remains
`FAILED_FOR_THIS_PAIR` and was not retried. Overall P1-10 remains
**IMPLEMENTED / AWAITING INDEPENDENT REVIEW**; this pair result is not
benchmark, pilot, multi-seed evidence, FormalRun acceptance, or Phase 1
acceptance.

The 2026-09-03 X11 failure was diagnosed as **ENVIRONMENT BLOCKED**. `DISPLAY=:0`
and `XAUTHORITY=/run/user/1000/gdm/Xauthority` were identical in the current
and exact harness child environments; `xdpyinfo` failed in both, while the
declared X0 socket was not reachable. The lower-level display cause remains
UNKNOWN. No code change was justified. See
[`x11_preflight_failure_diagnosis_20260903.md`](../evidence/P1-10/x11_preflight_failure_diagnosis_20260903.md).

The follow-up execution-context comparison found the known-good graphical
terminal had `xdpyinfo rc=0`, while the current Execution and exact child
environment had the same `DISPLAY=:0` and Xauthority path but `xdpyinfo rc=1`;
current X0 was not reachable. Namespace/credential sub-cause remains
UNKNOWN because the known-good namespace and uid/gid were not archived. The
minimal prerequisite is launching Execution from the same reachable desktop
namespace/session and rechecking `xdpyinfo rc=0` under the exact child
environment. See
[`x11_execution_context_comparison_20260903.md`](../evidence/P1-10/x11_execution_context_comparison_20260903.md).

## Offline saved-record comparison closure — 2026-09-03

The new pair
`P1-10-REPLAY-20260903-saved-record-closure-flat_goal_forward-stabilized`
is frozen in
`docs/evidence/P1-10/replay_pair_20260903_saved_record_closure/` with final
manifest SHA-256
`86ae55914db294d269d6f70909bfad1878c287c644f5a85c4075fa758f923a6c`.
This is a new pair and is not a retry of either historical
`replay_pair_20260902` or `replay_pair_20260903`. It is offline-only and
`FROZEN_OFFLINE_PENDING_INDEPENDENT_REVIEW`; no Operator A/B launch is
authorized before independent review accepts the closure.

The pair freezes `flat_goal_forward`, `stabilized`, root seed `20260902`,
`25.0 s`, `scene_default / mj_makeData:qpos0`, the accepted P1-08 v2 baseline,
the current P1-10 suite, and the exact/numeric/excluded saved-record
comparison rules. The repaired
`scripts/p1_10_saved_record_compare.py` accepts only `--pair-dir`, derives
`run_A`/`run_B` from the manifest, reads all four required files in each run,
validates process facts, terminal type/value domains, artifact origin, session
identity and complete binding without fallback, refuses live shared-memory
access and output overwrite, and emits
deterministic canonical input/output, JSON diff, and Markdown report. It does
not launch runtime processes.

The prior comparator contract was **REJECTED** because it did not force-read
process facts/context files and permitted external run paths. The repair scope
and evidence are in
[`comparator_contract_closure_20260903.md`](../evidence/P1-10/replay_pair_20260903_saved_record_closure/comparator_contract_closure_20260903.md).
Production context writer `scripts/p1_08_baseline_capture.py::write_p1_10_context`
now emits the full resolved scene binding into `p1_10_context.json`; no
comparator, pair, default, or runtime fallback is used. Offline comparator tests
are **17 test methods PASS**, including the producer-to-consumer closure. The corrected Operator runbook is
[`operator_replay_runbook.md`](../evidence/P1-10/replay_pair_20260903_saved_record_closure/operator_replay_runbook.md).
The baseline identity document SHA-256 is
`6c3563c25d45cc275db6b083f9f0fc0cc2067b48bc8f4a93dcace9f6d42817ea`; the
canonical baseline identity is
`59dd13fed5ebd026ec519f2659643237502be8e4d8df5174a65b7d35ceb4f7e0`.
No runtime records or comparison outputs exist yet; benchmark, FormalRun,
P1-11/P1-12/P1-13, commit, and push remain out of scope. The pair remains
`FROZEN_OFFLINE_PENDING_INDEPENDENT_REVIEW`; Operator A/B remains prohibited
until independent Reviewer approval.

## Residual-process preflight closure

The failed authorized pair exposed that the previous detector used
`pgrep -af "unitree_mujoco|ros2 launch|ros2_control_node"`, which matched the
current sandbox command line solely because it mentioned a simulator path.
The harness now reads `/proc/<pid>/exe`, `/proc/<pid>/cmdline`, and
`/proc/<pid>/stat` and excludes its own PID and ancestor chain. It only matches
the exact MuJoCo executable or the attributable ROS launch/controller
identities described above. Any process-table read failure, malformed record,
or controller identity ambiguity returns `uncertain` and is rejected before
capture. Focused offline coverage is recorded in `test_p1_08_harness.py`; the
current result is **93 checks PASS**.

The residual-process repair sub-gate is independently accepted with known
issues. Overall P1-10 remains **IMPLEMENTED / AWAITING INDEPENDENT REVIEW**;
the latest offline pair remains frozen pending independent review, while the
behavioral-validation Stage B/C gates remain open. No replay is performed in
this documentation synchronization; the historical runtime pairs remain
`FAILED_FOR_THIS_PAIR` and were not retried. See
[`REVIEW_2026-09-02_RESIDUAL_PROCESS_PREFLIGHT.md`](../evidence/P1-10/REVIEW_2026-09-02_RESIDUAL_PROCESS_PREFLIGHT.md).

## Evidence

- [`scenario_suite_validation.json`](../evidence/P1-10/scenario_suite_validation.json)
- [`initial_state_probe.json`](../evidence/P1-10/initial_state_probe.json)
- [`resolved_flat_goal_forward.json`](../evidence/P1-10/resolved_flat_goal_forward.json)
- [`resolved_flat_goal_lateral.json`](../evidence/P1-10/resolved_flat_goal_lateral.json)
- [`seed_propagation_evidence.json`](../evidence/P1-10/seed_propagation_evidence.json)
- [`P1-10_offline_verification.md`](../evidence/P1-10/P1-10_offline_verification.md)
- [`residual_process_preflight_closure_20260902.md`](../evidence/P1-10/residual_process_preflight_closure_20260902.md)
- [`residual_process_preflight_closure_20260902.json`](../evidence/P1-10/residual_process_preflight_closure_20260902.json)
- [`REVIEW_2026-09-02_RESIDUAL_PROCESS_PREFLIGHT.md`](../evidence/P1-10/REVIEW_2026-09-02_RESIDUAL_PROCESS_PREFLIGHT.md)
- [`replay_pair_20260903/pair_manifest.json`](../evidence/P1-10/replay_pair_20260903/pair_manifest.json)
- [`replay_pair_20260903/scenario_resolved_manifest.json`](../evidence/P1-10/replay_pair_20260903/scenario_resolved_manifest.json)
- [`replay_pair_20260903/run_A_preflight_fail.json`](../evidence/P1-10/replay_pair_20260903/run_A_preflight_fail.json)
- [`replay_pair_20260903/run_A_execution_facts.json`](../evidence/P1-10/replay_pair_20260903/run_A_execution_facts.json)
- [`replay_pair_20260903/pair_result.json`](../evidence/P1-10/replay_pair_20260903/pair_result.json)
- [`replay_pair_20260903/replay_pair_report.md`](../evidence/P1-10/replay_pair_20260903/replay_pair_report.md)
- [`replay_pair_20260903_saved_record_closure/pair_manifest.json`](../evidence/P1-10/replay_pair_20260903_saved_record_closure/pair_manifest.json)
- [`replay_pair_20260903_saved_record_closure/operator_replay_runbook.md`](../evidence/P1-10/replay_pair_20260903_saved_record_closure/operator_replay_runbook.md)
- [`p1_10_saved_record_compare.py`](../../scripts/p1_10_saved_record_compare.py)
- [`test_p1_10_saved_record_compare.py`](../../scripts/test_p1_10_saved_record_compare.py)

## Stage B/C offline preparation — historical five-map formalization

On 2026-09-03 Execution completed the offline preparation for the five
historical obstacle roots without changing the accepted flat suite or frozen
flat pair. The machine-readable inventory is
[`historical_five_map_inventory_20260903.json`](../evidence/P1-10/historical_five_map_inventory_20260903.json),
and the independent candidate suite is
[`scenarios/p1_10/obstacle_candidate_suite_manifest.json`](../../scenarios/p1_10/obstacle_candidate_suite_manifest.json).
The inventory recursively binds XML includes and file-backed assets, records
root/closure hashes and sizes, and records every root obstacle geom by stable
XML order. `scene_obstacle.xml` is byte-identical to `scene_test1.xml` and is
an alias of `obstacle_test1`, not an additional map. `scene_terrain.xml` is
registered only as an extra future/generalization candidate.

The five formal candidate identities are `obstacle_test1` through
`obstacle_test5`; all remain outside the accepted capture suite. They bind the fixed goal,
`stabilized_switch`, root seed `20260902` as provenance only, `25.0 s`,
`scene_default / mj_makeData:qpos0`, accepted P1-08 identity, scene root and
full closure, and XML-derived obstacle metadata. `obstacle_test1` additionally
has an independently reviewed offline collision source contract, but remains
**IMPLEMENTED / AWAITING RUNTIME VALIDATION**. `obstacle_test2`–`obstacle_test5`
remain `UNSUPPORTED`. No candidate is runtime-accepted because terminal
authority and obstacle repeatability are not closed, and the current capture
preflight intentionally remains the accepted flat suite. The flat suite hash
and frozen flat pair were not rewritten.

### Stage A

Flat replay validates infrastructure, repeatability, and the comparison chain.
It is only an internal sub-gate, not algorithm-validity proof, P1-10 final
acceptance, or authorization for P1-11.

### Stage B

`obstacle_test1` is the first candidate priority after de-duplication. Before
any runtime authorization, freeze its candidate binding and perform one real
behavioral run only after an independent review accepts the proposed source
contracts. The run must demonstrate real, explainable runtime sources for ray,
RA, Agile/Recovery switching, collision, terminal/outcome, and runtime record.
The offline source trace classifies geom→ray→RA→switching→Recovery as
implemented source trace. Collision is an independently accepted offline
authority implementation for future `obstacle_test1` captures; goal/fall/timeout
remain `UNKNOWN` or source-trace-only. Static obstacle XML presence alone is
not a collision event.

### Stage C

After Stage B authority and behavior evidence are closed, expand
`obstacle_test2`–`obstacle_test5` using the same hash-bound schema and form the
formal multi-obstacle scenario suite. Stage A passing does not equal P1-10
final acceptance; P1-11 cannot be authorized from Stage A alone. The final
number of obstacle maps is not fixed by this plan.

The detailed offline record is
[`historical_five_map_formalization_20260903.md`](../evidence/P1-10/historical_five_map_formalization_20260903.md).
No obstacle runtime, benchmark, FormalRun, P1-11, P1-12, or P1-13 was started.
Overall P1-10 remains **IMPLEMENTED / AWAITING INDEPENDENT REVIEW**.

## Stage B capture-binding/runtime-fingerprint repair — 2026-09-03

Independent-review REJECT blockers were repaired in the offline authority
implementation. Future captures receive a harness-generated, non-CLI
`p1-10-capture-<32 lowercase hex>` identity. It is bound through the resolved
context and controlled launch environment to collision snapshot v2, runtime
record, and process facts; the recorder rejects missing, malformed, stale,
old-version, or mismatched identities. The existing StateRL internal session
identifier is retained separately.

The simulator and offline construction probe share the canonical
`abs-go2-collision-model-fingerprint/v1` serialization: every loaded geom in
ID order, including obstacle and non-obstacle geoms, contributes type, body
ownership, group, contype, conaffinity, binary64 pos/quat/size, and geom/body
names. `closure_sha256` is the preflight file identity; the runtime fingerprint
is the loaded MuJoCo contact-model identity. Authority requires both plus the
capture binding. The instrumented executable is a new Stage-B artifact and
must receive its own manifest/identity; the accepted P1-08 executable and
frozen flat pair were not rewritten.

The formal collision publish scope is the two harness-controlled `main.cc`
PhysicsLoop `mj_step` paths. UI step-forward in `simulate.cc` is interactive
debugging only, outside formal P1-10 capture; the Operator runbook prohibits
UI reset/keyframe/teleop intervention. Goal/fall/timeout remain UNKNOWN or
source-trace-only where previously documented. No runtime capture was made.

Final independent review: **ACCEPT WITH KNOWN ISSUES / STAGE-B OBSTACLE RUNTIME
PREPARATION MAY PROCEED**. The accepted review is
[`REVIEW_2026-09-03_STAGE_B_AUTHORITY.md`](../evidence/P1-10/REVIEW_2026-09-03_STAGE_B_AUTHORITY.md).
Obstacle runtime authorization remains withheld pending a separate Stage-B
execution manifest/identity and Director authorization.

## Stage B authority implementation — 2026-09-03

Director-authorized offline work added the minimum collision authority for the
canonical `obstacle_test1` model. The simulator physics loop now has a
versioned, fail-closed `/mujoco_collision_v2` snapshot source after the existing
`mj_step` calls. It binds the scene by the frozen root/closure identity and all
seven XML-derived obstacle signatures, reads `mjData::ncon`/`mjContact`, and
classifies only robot↔bound-obstacle contacts as formal obstacle collision.
Floor, self-contact, other, unknown, stale, malformed, or mismatched data do
not become `collision=false`. The legacy `/mujoco_collision` integer telemetry
is compatibility-only and is not formal authority.

The existing saved runtime recorder consumes the versioned snapshot and writes
its structured data into `runtime_record.jsonl`, including timestamp,
simulation-time, physics-step, class counts, identity, authority, and coverage.
An observed valid robot↔obstacle contact may be recorded as
`collision_events=true`. A final sampled no-contact value is never promoted to
episode-wide `collision_events=false` because the recorder has no complete
episode coverage boundary. Historical records without the optional snapshot
remain compatible and explicitly UNKNOWN.

Goal arrival is only source-trace-only and is not promoted to formal terminal
authority; fall remains UNKNOWN; the fixed 25 s capture window is not a
controller timeout and remains UNKNOWN. No new thresholds or control behavior
were introduced. No runtime capture was performed, so `obstacle_test1` remains
`IMPLEMENTED / AWAITING RUNTIME VALIDATION`, and all five historical obstacle
candidate maps remain outside the accepted suite. Evidence:
[`stage_b_collision_terminal_authority_20260903.md`](../evidence/P1-10/stage_b_collision_terminal_authority_20260903.md).

Before the fingerprint repair, the obstacle candidate suite had been refreshed
only to bind the source status:
`scenarios/p1_10/obstacle_candidate_suite_manifest.json` SHA-256
`4552fc5b408855174337c7b2d73acf49a92a5b44622a540e0d1b90082ab58cb5`. The
scenario itself remains `UNSUPPORTED` and cannot enter capture; only the
`obstacle_test1` nested collision-authority binding is
`IMPLEMENTED / AWAITING RUNTIME VALIDATION`. `obstacle_test2`–`obstacle_test5`
remain unsupported. The accepted flat suite and frozen flat pair are unchanged.

The Stage B authority implementation is **IMPLEMENTED / AWAITING INDEPENDENT
REVIEW**. Independent review and a separately authorized runtime validation are
required before an Operator may run the obstacle scenario. This work does not
constitute P1-10 acceptance, benchmark/FormalRun evidence, ABS obstacle
effectiveness, or authorization for P1-11/P1-12/P1-13.
