  测试（3 终端，同之前）：

  # 终端1：MuJoCo
  cd ~/quadruped_robots/unitree_mujoco && export LD_LIBRARY_PATH=/home/lidio/Libraries/unitree_sdk2/lib:$LD_LIBRARY_PATH && ./simulate/build2/unitree_mujoco

  # 终端2：RL控制器
  source /opt/ros/humble/setup.bash && source ~/quadruped_robots/quadruped_ros2_control_humble/install/setup.bash && export
  LD_LIBRARY_PATH=/home/lidio/Libraries/unitree_sdk2/lib:/home/lidio/Libraries/libtorch-cpu-2.0.1/lib:$LD_LIBRARY_PATH && ros2 launch rl_quadruped_controller mujoco.launch.py

  # 终端3：键盘
  source /opt/ros/humble/setup.bash && source ~/quadruped_robots/quadruped_ros2_control_humble/install/setup.bash && ros2 run keyboard_input keyboard_input

  操作：W×20 → 2 → 2 → 3 → 继续按 W。看 debug 日志中 cmd= 是否在 [1.5, 7.5] 范围，以及 MuJoCo 中机器人是否走直线。