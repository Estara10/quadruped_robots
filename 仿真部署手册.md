# 仿真部署手册

Go2 + ABS 在 MuJoCo + ROS2 Humble 中的运行、调试和场景测试说明。

**最后更新**：2026-07-09

---

## 1. 架构

```text
MuJoCo C++ 仿真器
  ├─ 物理步进
  ├─ LowCmd 订阅
  ├─ LowState / SportModeState 发布
  ├─ 几何 ray2d → /mujoco_ray2d
  └─ qpos → /mujoco_qpos
        │ DDS(domain=1, lo)
        ▼
ros2_control_node
  └─ hardware_unitree_mujoco
        │ command/state interfaces
        ▼
rl_quadruped_controller / StateRL.cpp
  ├─ Agile policy
  ├─ RA value network
  ├─ Recovery policy
  ├─ recovery twist GD
  └─ 目标导航 + recovery-aware 直线路径约束
```

---

## 2. 仿真/真机模式切换

仿真模式下 `ros2_control.xacro` 应注释网络参数，让硬件接口默认使用：

```text
network_interface = lo
domain = 1
```

仿真模式：

```xml
<!--<param name="domain">0</param>-->
<!--<param name="network_interface">enp7s0</param>-->
```

真机模式：

```xml
<param name="domain">0</param>
<param name="network_interface">enp7s0</param>
```

每次切换后都要编译：

```bash
cd ~/quadruped_robots/quadruped_ros2_control_humble
source /opt/ros/humble/setup.bash
colcon build --packages-select go2_description --symlink-install
```

---

## 3. 快速启动

### 3.1 自动避障演示

```bash
cd ~/quadruped_robots
source /opt/ros/humble/setup.bash
source quadruped_ros2_control_humble/install/setup.bash
export LD_LIBRARY_PATH=/home/lidio/Libraries/unitree_sdk2/lib:/home/lidio/Libraries/libtorch-cpu-2.0.1/lib:$LD_LIBRARY_PATH
MUJOCO_SCENE=scene_obstacle.xml ./scripts/launch_abs_obstacle.sh
```

脚本会自动：

```text
PASSIVE -> FIXEDDOWN -> FIXEDSTAND -> RL
```

并发送 `ly=1.0`，所以目标点会在 YAML `goal_x` 基础上前移 2m。

### 3.2 平地直线测试

```bash
MUJOCO_SCENE=scene_flat.xml ./scripts/launch_abs_obstacle.sh
```

看 `[EVAL] robot=(x,y)`，重点看中途 `y` 是否过大。

### 3.3 手动 FSM 测试

```bash
AUTO_RL=false MUJOCO_SCENE=scene_flat.xml ./scripts/launch_abs_obstacle.sh
```

另开终端：

```bash
cd ~/quadruped_robots/quadruped_ros2_control_humble
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 run keyboard_input keyboard_input
```

按键：

```text
2 -> FIXEDDOWN
2 -> FIXEDSTAND
3 -> RL
4 -> 手动 RL_REC
1 -> PASSIVE/卸力
9 -> HARD STOP/卸力急停
W/S/A/D/J/L -> 速度/目标微调
Space -> 清零输入
```

---

## 4. 目标导航与直线路径约束

### 4.1 基础目标导航

`abs/config.yaml` 中：

```yaml
abs:
  goal_x: 7.0
  goal_y: 0.0
  resample_goal_on_arrival: false
```

机器人使用 odometer 世界坐标和 IMU yaw，将世界目标转换为机体系 commands。

到达阈值：

```text
dist < 0.5m -> commands = 0 -> 站住
```

### 4.2 recovery-aware 直线路径约束

为避免“最终能到目标，但途中偏离路径撞到旁边障碍”，现在加入路径跟踪：

```yaml
path_tracking_enabled: true
path_lateral_gain: 1.5
path_heading_gain: 1.5
```

逻辑：

```text
非 recovery：压住起点→目标直线
进入 recovery：path_on=0，允许离开直线避障
退出 recovery：path_on=1，重新拉回直线
```

日志：

```text
[PATH] start=(...) goal=(...) tracking=1 lateral_gain=1.50 heading_gain=1.50
[GOAL] ... path_err=... path_on=1
```

---

## 5. RA / Recovery

RA 值网络：

```text
19-dim = lin_vel(3) + ang_vel(3) + commands[0:2](2) + ray2d(11)
```

触发：

```text
ra > -0.05 -> enter recovery
ra < -0.08 且 hold 结束 -> exit recovery
```

Recovery twist 优化严格按论文 ROS1 参考：

```text
3 次梯度下降
loss = 10 * max(ra + 0.1, 0) + 0.02 * pos_dev^2
```

保持期：

```yaml
recovery_hold_steps: 30
```

日志：

```text
[RA]
[RA-REC] 进入恢复
[TWIST-GD]
[REC-DIAG]
[RA-REC] 退出恢复
```

---

## 6. ray2d

仿真正式评估使用 MuJoCo 几何 ray2d：

```text
11 条射线，-45° 到 +45°，间隔 9°
距离 log2 变换
共享内存：/mujoco_ray2d
```

切换外部 Ray-Pred：

```bash
MUJOCO_RAY_SOURCE=ray_pred ./scripts/launch_abs_ray_pred.sh
```

当前建议：

- 仿真稳定性/避障评估：用几何 ray
- Ray-Pred：仅实验/对比，不作为正式闭环默认
- 真机：必须先接入真实 D435i/LiDAR/深度相机 ray2d，才能跑 ABS/RL

---

## 7. 场景

常用场景：

| 场景 | 适合用途 |
|---|---|
| `scene_flat.xml` | 平地直线、速度、偏航 |
| `scene_obstacle.xml` | 标准避障，最推荐 |
| `scene_test2.xml` | 左右均衡障碍 |
| `scene_rand_03.xml` | 中线障碍多，容易触发 recovery |
| `scene_rand_08.xml` | 中线障碍多，偏难 |
| `scene_slope.xml` | 坡地稳定性 |
| `scene_slope_obstacle.xml` | 坡地 + 障碍，高难度 |
| `scene_terrain.xml` | 复杂地形，当前不建议作为基础验证 |

完整说明见 `scene.txt`。

---

## 8. 关键日志检查

启动时应看到：

```text
[VERIFY] joint interface order: FR_hip_joint FR_thigh_joint FR_calf_joint ... RL_calf_joint
[MOTOR-MAP] controller[0] FR_hip_joint -> Unitree motor[0]
```

平地直线重点看：

```text
[EVAL] robot=(x,y) yaw=...
[GOAL] ... path_err=... path_on=1
```

避障重点看：

```text
[RA-REC] 进入恢复
path_on=0
[RA-REC] 退出恢复
path_on=1
```

---

## 9. 构建

```bash
cd ~/quadruped_robots/quadruped_ros2_control_humble
source /opt/ros/humble/setup.bash
colcon build --packages-select hardware_unitree_mujoco rl_quadruped_controller keyboard_input go2_description --symlink-install \
  --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DTorch_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Torch
```
