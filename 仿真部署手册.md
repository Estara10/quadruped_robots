# 仿真部署手册

Go2 四足机器人 MuJoCo 仿真 + ROS2 RL 策略部署完整指南

**最后更新**: 2026-06-09

---

## 一、系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│ terminal 2 (ROS2)                                               │
│                                                                 │
│  ros2 launch rl_quadruped_controller mujoco.launch.py           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ ros2_control_node                                         │  │
│  │   └─ hardware_unitree_mujoco (DDS 桥接插件)               │  │
│  │       ├─ export_command_interfaces: position/vel/eff/kp/kd│  │
│  │       ├─ export_state_interfaces: joint/IMU/foot_force    │  │
│  │       │       /odometer (世界坐标位置)                     │  │
│  │       ├─ write() → LowCmd → DDS channel                   │  │
│  │       └─ read()  ← LowState/SportModeState ← DDS channel  │  │
│  └───────────────────────────────────────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────────┘
                           │ DDS (domain_id=1, interface=lo)
┌──────────────────────────┴──────────────────────────────────────┐
│ terminal 1 (MuJoCo)                                              │
│                                                                  │
│  ./simulate/build2/unitree_mujoco                                │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ unitree_sdk2_bridge                                        │  │
│  │   ├─ subscribe LowCmd → 驱动 MJCF 模型 (PD 控制)           │  │
│  │   ├─ MuJoCo 物理步进 (mj_step)                             │  │
│  │   ├─ computeRay2d() → 11条射线 → /mujoco_ray2d (shm)       │  │
│  │   ├─ 写入 qpos → /mujoco_qpos (shm, 19 doubles)            │  │
│  │   └─ publish LowState/SportModeState → DDS channel          │  │
│  └────────────────────────────────────────────────────────────┘  │
│  MuJoCo GUI (3D 渲染窗口)                                        │
└──────────────────────────────────────────────────────────────────┘
```

## 二、快速启动

### 一键启动（平坦地面）

```bash
~/quadruped_robots/scripts/launch_abs_sim.sh
```

自动完成：启动 MuJoCo → ROS2 Controller → 进入 RL 模式。默认到达第一个 YAML 配置目标后站住，不再重采样新目标。

### 一键启动（障碍物场景）

```bash
MUJOCO_SCENE_OVERRIDE=scene_test1.xml ~/quadruped_robots/scripts/launch_abs_terrain.sh
```

可用场景：`scene_test1.xml` ~ `scene_test5.xml`, `scene_terrain.xml`

### 手动三终端启动

**Terminal 1** — MuJoCo 物理引擎：
```bash
cd ~/quadruped_robots/unitree_mujoco
export LD_LIBRARY_PATH=/home/lidio/Libraries/unitree_sdk2/lib:$LD_LIBRARY_PATH
./simulate/build2/unitree_mujoco                        # 默认场景 (scene.xml)
./simulate/build2/unitree_mujoco -s scene_terrain.xml   # 指定场景
```

**Terminal 2** — ROS2 控制器：
```bash
source /opt/ros/humble/setup.bash
source ~/quadruped_robots/quadruped_ros2_control_humble/install/setup.bash
export LD_LIBRARY_PATH=/home/lidio/Libraries/unitree_sdk2/lib:/home/lidio/Libraries/libtorch-cpu-2.0.1/lib:$LD_LIBRARY_PATH
ros2 launch rl_quadruped_controller mujoco.launch.py
```

**Terminal 3** — 键盘控制或自动进入 RL：
```bash
# 方式 A: 键盘手动控制
source /opt/ros/humble/setup.bash
source ~/quadruped_robots/quadruped_ros2_control_humble/install/setup.bash
ros2 run keyboard_input keyboard_input
# 按键: 2→2→3 (进入RL), W/S=前进/后退, A/D=左右, 4=手动Recovery

# 方式 B: 自动进入 RL
ros2 topic pub --once /control_input control_input_msgs/msg/Inputs "{command: 2, lx: 0.0, ly: 0.0, rx: 0.0, ry: 0.0}"
sleep 1
ros2 topic pub --once /control_input control_input_msgs/msg/Inputs "{command: 2, lx: 0.0, ly: 0.0, rx: 0.0, ry: 0.0}"
sleep 4
ros2 topic pub --once /control_input control_input_msgs/msg/Inputs "{command: 3, lx: 0.0, ly: 0.0, rx: 0.0, ry: 0.0}"
```

## 三、系统功能说明

### 3.1 目标点导航

机器人从 odometer 传感器获取世界坐标位置，从 IMU 四元数提取 yaw 角，
计算世界坐标系目标点在机体坐标系下的位置偏移和航向误差，
作为 61 维观测中的 commands[0:3] 输入策略。

目标点通过 YAML 配置（`abs/config.yaml`）：
```yaml
abs:
  goal_x: 7.0                      # 世界坐标 X (前方, 米)
  goal_y: 0.0                      # 世界坐标 Y (左侧为正)
  resample_goal_on_arrival: false   # false=到达首目标后停止, true=连续随机目标
```

键盘 W/S 可微调目标 ±2m，A/D 微调侧向 ±2m。注意 `launch_abs_sim.sh` 自动进入 RL 时会发送 `ly=1.0`，因此首目标会在 `goal_x` 基础上额外前移 2m；结构化评估脚本默认使用 `--command-ly 0.0`，目标就是 YAML 的 `goal_x=7.0m`。

### 3.2 到达检测与目标重采样

当机器人距离目标小于 0.5m（训练 sigma_tight），commands 置零告知策略站立。
默认 `resample_goal_on_arrival: false`，因此一键仿真到达第一个目标后会停住，适合验证直线性和单次到达。
若将该配置改为 `true`，站立约 1.6 秒后会在当前位置前方 [1.5, 7.5]m、侧向 [-2.0, 2.0]m 随机采样下一个目标点。

日志标识：`[ARRIVED]`；开启连续目标时还会出现 `[GOAL-RESAMPLE]`

### 3.3 RA 值与恢复触发

RA 值网络 (19→64→64→1 Tanh) 每 RL 步推理一次。
当 RA 值超过 -0.05 时触发恢复策略，低于 -0.08 时退出。

RA 观测构造 (19 维)：lin_vel(3) + ang_vel(3) + commands[0:2](2) + ray2d(11)。其中 `lin_vel` 必须是机体系速度；MuJoCo 运行时优先读取 odometer world velocity，再用 IMU 四元数旋转到 body frame。

日志标识：`[RA]` `[RA-REC] ENTER/EXIT`

### 3.4 Recovery Twist 优化

严格遵照论文方法（ROS1 depth_obstacle_depth_goal_ros.py L498-525）：

1. 初始化 twist = [当前vx, 当前vy, 当前wz]，设置 requires_grad=True
2. 三次梯度下降迭代：
   - 构造含当前 twist 的 19 维 RA 观测
   - 前向 RA 模型得 ra 值
   - loss = 10 × max(ra + 0.1, 0) + 0.02 × ((twist.vx×0.05 - cmd_x)² + (twist.vy×0.05 - cmd_y)²)
   - backward() + clip_grad(±1.0) + lr=0.5 更新 + clip twist 到范围
3. 进入恢复保持期（30 RL 步 ≈ 240ms），期间复用缓存 twist
4. 保持期满且 ra < -0.08 时退出

频率适配：ROS2 RL 推理频率为 125 Hz（每步 8ms），较 ROS1 的 12.5 Hz（每步 80ms）快 10 倍。
30 步保持期匹配 ROS1 约 3 步的有效恢复时长。

保持步数可通过 YAML 配置：`recovery_hold_steps: 30`

日志标识：`[TWIST-GD]`

### 3.5 射线感知

MuJoCo 桥以 2D 射线圆相交算法计算 11 条射线距离（-45° 到 +45°，步长 9°），
经 log2 变换写入共享内存 `/mujoco_ray2d`。

geom 过滤机制：跳过 plane(0), hfield(1), mesh(7) 类型及机器人 geom (group 2/3) 和动态体。
仅对显式障碍物 geom (box, cylinder, sphere, capsule, ellipsoid) 进行射线检测。

### 3.6 深度相机 + ResNet18 管线（实验性）

Python 脚本 `scripts/ray_predictor.py` 实现论文的深度感知管线：
MuJoCo Python 渲染 160×90 深度图 → log2 变换 → ResNet18 TorchScript 推理
→ 11 个 log2 射线距离写入 shm。

当前状态：管线可运行（推理 ~14ms），但模型用 ZED 实机深度数据训练，
与 MuJoCo 渲染深度存在分布差异，射线质量不足以支撑可靠避障。
需域适应或仿真重训练后启用。

## 四、场景管理

### 可用场景

| 场景文件 | 内容 | 用法 |
|---------|------|------|
| `scene.xml` | 平坦地面（无任何障碍物）| 默认 |
| `scene_terrain.xml` | 复杂地形 + 多种障碍物 | 验证避障 |
| `scene_test1~5.xml` | 随机生成的 box + cylinder 障碍物 | 多场景测试 |

### 生成新测试场景

```bash
conda activate abs
python ~/quadruped_robots/scripts/generate_test_scenes.py
```

生成 5 个随机场景到 `unitree_mujoco/unitree_robots/go2/scene_test1~5.xml`。

### 多场景快速测试

```bash
~/quadruped_robots/scripts/test_multiscene.sh
```

依次加载 6 个场景，每个运行 25 秒，统计 recovery 触发和到达次数。
该脚本适合快速肉眼检查，不生成结构化评估报告。

### 结构化仿真评估

```bash
# 先编译控制器，确保 [EVAL] 遥测已生效
cd ~/quadruped_robots/quadruped_ros2_control_humble
source /opt/ros/humble/setup.bash
colcon build --packages-select rl_quadruped_controller go2_description --symlink-install \
  --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DTorch_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Torch

# 固定场景基线：每个场景跑 1 次，先确认流程
# 默认 command_ly=0.0，不额外平移目标，评估目标为 YAML 中的 goal_x=7.0m
cd ~/quadruped_robots
python3 scripts/run_abs_eval.py --runs-per-scene 1 --duration 30

# 汇总最近一次 session
python3 scripts/analyze_abs_eval.py --input logs/abs_eval/<session_name>
```

输出目录：

```text
logs/abs_eval/<session_name>/
├── session_summary.csv
├── session_summary.json
├── aggregate_report.csv
├── aggregate_report.md
└── scene_test1/
    └── run_001/
        ├── runtime.log
        ├── telemetry.csv
        └── summary.json
```

推荐第一轮基线：

```bash
python3 scripts/run_abs_eval.py \
  --scenes scene.xml scene_terrain.xml scene_test1.xml scene_test2.xml scene_test4.xml scene_test5.xml \
  --runs-per-scene 10 \
  --duration 45 \
  --command-ly 0.0
```

窄通道压力测试单独统计：

```bash
python3 scripts/run_abs_eval.py \
  --scenes scene_test3.xml \
  --runs-per-scene 10 \
  --duration 45 \
  --command-ly 0.0
```

`run_abs_eval.py` 会自动启动 MuJoCo、ROS2 控制器、发送 `2 → 2 → 3 → 3` 进入 RL，
并读取 `/dev/shm/mujoco_qpos`、`/dev/shm/mujoco_ray2d` 作为 episode 级监控信号。
评估依赖 `[EVAL]` 遥测：重点检查 `body`, `heading`, `ra`, `recovery`, `min_ray_m`, `lin_vel`。
标准基线使用 `--command-ly 0.0`，不通过 joystick trim 额外平移目标，因此实际目标为 YAML 的 `goal_x=7.0m`；
若使用 `--command-ly 1.0`，目标会变为 `7.0 + 2.0 = 9.0m`，超出训练 forward 目标范围上界。
脚本会对场景做出生点净空预检查：默认要求障碍物 footprint 距原点至少 0.75m，
并警告 `x<=1.5m, |y|<=0.45m` 的初始前向走廊障碍。
报告会把 `scene_test3.xml` 归入 pressure test，因为该场景主要测试窄通道下的 recovery-loop 行为；
其他场景归入 standard baseline。`STUCK_RECOVERY_LOOP` 表示 episode 超时、recovery 占比高、recovery 次数多且目标距离没有有效下降，
应作为局部导航/安全规则问题分析，不与普通超时混淆。
如果场景本身把障碍放在出生点附近，例如 `scene_test4.xml` 的障碍前缘约 0.15m，
这类失败应归类为 `INVALID_SCENE_SPAWN` 或 `invalid_spawn_failure`，不计为 ABS 策略失败。
可用 `--skip-invalid-spawn` 在批量评估时直接跳过此类场景。
当前 `collision_proxy` 是基于前向 ray2d 的近碰撞代理指标，不等同于 MuJoCo 真实接触对；
如果后续需要严格碰撞率，应在 MuJoCo bridge 中增加 contact pair 遥测。

## 五、编译命令

```bash
cd ~/quadruped_robots/quadruped_ros2_control_humble
source /opt/ros/humble/setup.bash

# 编译 RL 控制器（包含 TorchScript 推理）
colcon build --packages-select rl_quadruped_controller --symlink-install \
  --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DCMAKE_PREFIX_PATH="/home/lidio/Libraries/libtorch-cpu-2.0.1:/opt/ros/humble" \
  -DTorch_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Torch \
  -DCaffe2_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Caffe2

# 编译配置变更（无 C++ 改动时）
colcon build --packages-select go2_description --symlink-install

# 编译 MuJoCo 仿真器
cd ~/quadruped_robots/unitree_mujoco/simulate/build2
make -j$(nproc) unitree_mujoco
```

## 六、关键配置参数

### abs/config.yaml

| 参数 | 值 | 说明 |
|------|-----|------|
| `goal_x / goal_y` | 7.0 / 0.0 | 世界坐标系目标点 |
| `resample_goal_on_arrival` | false | 到达首目标后停止；设为 true 才连续随机目标 |
| `ra_threshold` | -0.05 | RA 触发进入阈值 |
| `contact_threshold` | 1.0 | 足力接触检测 (N, 匹配训练) |
| `recovery_hold_steps` | 30 | 恢复最小保持 RL 步数 |
| `soft_start_steps` | 250 | Kp/Kd 软启动步数 |
| `twist_lam` | 10.0 | 梯度下降 RA 惩罚系数 |
| `twist_lr` | 0.5 | 梯度下降学习率 |
| `twist_tau` | 0.05 | 位置积分时间常数 |
| `twist_eps` | 0.05 | RA 安全边际 |

## 七、日志说明

| 日志标签 | 含义 | 频率 |
|---------|------|------|
| `[GOAL]` | 机器人位置、目标、距离、到达标记 | 每 100 RL 步 |
| `[GOAL-RESAMPLE]` | 到达后采样新目标，仅 `resample_goal_on_arrival=true` 时出现 | 事件触发 |
| `[RA]` | RA 值网络推理结果 | 首次 |
| `[RA-REC] ENTER/EXIT` | 恢复策略触发/退出 | 事件触发 |
| `[TWIST-GD]` | 梯度下降优化 twist 过程 | 每次恢复触发 |
| `[REC-DIAG]` | 恢复策略动作范围 | 每 10 次 |
| `[EVAL]` | 结构化仿真评估遥测：目标距离、RA、recovery、ray2d、动作范围、接触 | 默认每 25 RL 步 |
| `[Ray2D]` | 射线共享内存状态 | MuJoCo 启动时 |

## 八、当前状态总览

### 仿真已完成

- 敏捷策略推理 (61-dim obs → 12 joint targets)
- 恢复策略推理 (49-dim obs, inline, 匹配论文)
- RA 值网络推理 (19→64→64→1 Tanh)
- 论文梯度下降 recovery twist 优化
- 频率适配恢复保持机制
- 世界坐标目标点导航
- 到达检测；默认首目标停止，可配置连续目标重采样
- RA/recovery 机体系速度修复
- Estimator 腿链顺序修复
- 射线感知 (geom 类型过滤)
- FSM 状态机自动启动
- DDS 超时检测 + 软启动

### 进行中

- 直线性与避障行为校准
- 多场景结构化评估
- 调试日志默认开关收敛

### 待完成

- 深度相机 + ResNet18 实机感知 (M6b, 需域适应)
- Go2 实机部署 (M8)
- 遥控器紧急停止 (E.2)
- 温度监控 (E.4)

## 九、安全机制

仿真中多项安全措施独立运行，任一触发即强制跳转 PASSIVE 状态（归零所有关节 Kp/Kd）。

| 安全机制 | 触发条件 | 依赖 | 日志标识 |
|---------|---------|------|---------|
| 机身姿态倾斜 | roll 或 pitch > 75° | IMU 四元数（不依赖 estimator） | `[SAFETY] Body tilt exceeded` |
| Estimator 姿态 | roll 或 pitch > 90° | Estimator（需 `enable_estimator_=true`） | 无显式日志，直接跳 PASSIVE |
| DDS 超时 | 关节位置连续 100 步无变化 | 关节状态接口 | `[EMERGENCY] DDS timeout` |
| 键盘急停 | command=1 发布到 /control_input | 键盘或脚本 | FSM 静默切换 |
| Action 输出限幅 | RL 输出超出 ±`action_output_clip` | `abs/config.yaml` 配置 | 无日志（静默 clamp） |
| 关节位置限幅 | 目标位置超出 Go2 关节范围 | URDF 关节限位 | 无日志（静默 clamp） |

配置项（`abs/config.yaml`）：
```yaml
abs:
  body_tilt_limit_deg: 75.0    # roll/pitch 超此值 → PASSIVE
  action_output_clip: 4.0      # RL action clamp ±4 (≈ ±1 rad 关节偏移)
```
