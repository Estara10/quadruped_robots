# 仿真部署手册

Go2 四足机器人 MuJoCo 仿真 + ROS2 RL 策略部署完整指南

---

## 一、系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│ terminal 2 (ROS2)                                               │
│                                                                 │
│  ros2 launch rl_quadruped_controller mujoco.launch.py           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ ros2_control_node                                         │  │
│  │   └─ hardware_unitree_mujoco (DDS 桥接插件)               │  │
│  │       ├─ export_command_interfaces: position/vel/eff/kp/kd│  │
│  │       ├─ export_state_interfaces: position/vel/eff/IMU    │  │
│  │       ├─ write() → LowCmd → DDS channel                   │  │
│  │       └─ read()  ← LowState ← DDS channel                 │  │
│  └───────────────────────────────────────────────────────────┘  │
│  robot_state_publisher (TF)  rviz2 (可视化)                      │
└──────────────────────────┬──────────────────────────────────────┘
                           │ DDS (domain_id=1, interface=lo)
┌──────────────────────────┴──────────────────────────────────────┐
│ terminal 1 (MuJoCo)                                              │
│                                                                  │
│  ./simulate/build2/unitree_mujoco -r go2                             │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │ unitree_sdk2_bridge                                        │  │
│  │   ├─ subscribe LowCmd → 驱动 MJCF 模型 (PD 控制)           │  │
│  │   ├─ MuJoCo 物理步进 (mj_step)                             │  │
│  │   └─ publish LowState/SportModeState → DDS channel          │  │
│  └────────────────────────────────────────────────────────────┘  │
│  MuJoCo GUI (3D 渲染窗口)                                        │
└──────────────────────────────────────────────────────────────────┘

终端 3: ros2 run keyboard_input keyboard_input (键盘控制)
```

**核心要点**:
- `unitree_mujoco` 运行 MuJoCo 物理引擎，对外暴露 DDS 通道（domain_id=1, lo 回环网卡）
- `hardware_unitree_mujoco` 是 ROS2 硬件接口插件，通过 DDS 桥接 MuJoCo → ROS2
- 两个进程通过 DDS (CycloneDDS) 在本地回环上通信，模拟真实机器人的 DDS 通信协议
- 启动顺序: 先 MuJoCo 仿真 → 再 ROS2 控制器

---

## 二、安装依赖

### 2.1 unitree_sdk2

```bash
git clone https://github.com/unitreerobotics/unitree_sdk2.git
cd unitree_sdk2
mkdir build && cd build
cmake .. -DCMAKE_INSTALL_PREFIX=/opt/unitree_robotics
sudo make install
```

### 2.2 MuJoCo

```bash
sudo apt install libglfw3-dev libxinerama-dev libxcursor-dev libxi-dev
```

```bash
git clone https://github.com/google-deepmind/mujoco.git
cd mujoco
mkdir build && cd build
cmake ..
make -j$(nproc)
sudo make install
```

测试安装:
```bash
simulate   # 弹出 MuJoCo 窗口 = 成功
```

### 2.3 yaml-cpp

```bash
sudo apt install libyaml-cpp-dev
```

### 2.4 ROS2 + libtorch (RL 策略推理)

```bash
# libtorch CPU 版 (已安装)
wget https://download.pytorch.org/libtorch/cpu/libtorch-cxx11-abi-shared-with-deps-2.5.0%2Bcpu.zip
unzip libtorch-cxx11-abi-shared-with-deps-2.5.0+cpu.zip

# 当前环境使用 2.0.1 CPU 版
# /home/lidio/Libraries/libtorch-cpu-2.0.1/
```

### 2.5 ROS2 包依赖

```bash
cd ~/quadruped_robots/quadruped_ros2_control_humble
rosdep install --from-paths src --ignore-src -r -y
```

---

## 三、编译

### 3.1 编译 unitree_mujoco (C++ 仿真器)

```bash
cd ~/quadruped_robots/unitree_mujoco/simulate
mkdir build && cd build
cmake .. -Dmujoco_DIR=/home/lidio/Libraries/mujoco-3.3.3-tmp/build \
         -Dunitree_sdk2_DIR=/home/lidio/Libraries/unitree_sdk2/lib/cmake/unitree_sdk2
make -j$(nproc)
```

产出: `./unitree_mujoco` 可执行文件 (当前位于 `simulate/build2/unitree_mujoco`)

### 3.2 编译 ROS2 控制栈

```bash
cd ~/quadruped_robots/quadruped_ros2_control_humble
source /opt/ros/humble/setup.bash
colcon build --packages-select rl_quadruped_controller --symlink-install \
  --cmake-args \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DCMAKE_PREFIX_PATH="/home/lidio/Libraries/libtorch-cpu-2.0.1:/opt/ros/humble" \
  -DTorch_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Torch \
  -DCaffe2_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Caffe2
```

编译全部（首次或依赖变更时）:
```bash
colcon build --packages-up-to rl_quadruped_controller --symlink-install \
  --cmake-args \
  -DCMAKE_PREFIX_PATH="/home/lidio/Libraries/libtorch-cpu-2.0.1:/opt/ros/humble" \
  -DTorch_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Torch \
  -DCaffe2_DIR=/home/lidio/Libraries/libtorch-cpu-2.0.1/share/cmake/Caffe2
```

---

## 四、启动仿真

### 4.1 启动 MuJoCo 物理引擎 (终端 1)

```bash
cd ~/quadruped_robots/unitree_mujoco/simulate/build2
./unitree_mujoco -r go2
```

看到 MuJoCo 窗口弹出，Go2 机器人站在地面上 → 成功。

配置文件: `~/quadruped_robots/unitree_mujoco/simulate/config.yaml`
```yaml
robot: "go2"
robot_scene: "scene.xml"       # 或 "scene_terrain.xml" 使用地形
domain_id: 1
interface: "lo"
use_joystick: 0                 # 无手柄时设为 0
print_scene_information: 1
```

### 4.2 启动 ROS2 控制器 (终端 2)

```bash
source /opt/ros/humble/setup.bash
source ~/quadruped_robots/quadruped_ros2_control_humble/install/setup.bash
export LD_LIBRARY_PATH="/home/lidio/Libraries/unitree_sdk2/lib:/home/lidio/Libraries/libtorch-cpu-2.0.1/lib:$LD_LIBRARY_PATH"
ros2 launch rl_quadruped_controller mujoco.launch.py
```

> **注意**: `LD_LIBRARY_PATH` 必须包含 `unitree_sdk2/lib` (提供 `libddsc.so.0`) 和 `libtorch`。
> 如果当前在 conda 环境 `abs` 中，conda 的 `LD_LIBRARY_PATH` 可能会覆盖系统路径。
> 建议在非 conda 终端或显式 export。

### 4.3 键盘控制 (终端 3)

```bash
source /opt/ros/humble/setup.bash
source ~/quadruped_robots/quadruped_ros2_control_humble/install/setup.bash
ros2 run keyboard_input keyboard_input
```

---

## 五、键盘按键映射

### 5.1 FSM 状态切换 (通用)

| 按键 | 状态 | 说明 |
|------|------|------|
| `1` | PASSIVE | 无力控制，机器人瘫倒 |
| `2` | FIXEDDOWN | 趴下位置 |
| `3` | FIXEDSTAND | 站立位置 |

### 5.2 RL 控制流程

```
终端 3 依次按键:
  2 → 等 1.5s → 2 → 等 1.5s → 3
  (FIXEDDOWN → 回到 FIXEDDOWN → RL 状态)

按键顺序说明:
  - 第一下 '2': PASSIVE → FIXEDDOWN (趴下)
  - 第二下 '2': FIXEDDOWN → FIXEDDOWN (状态不变，重新 enter)
  - '3':       FIXEDDOWN → FIXEDSTAND (站立) → RL (自动切换)
```

### 5.3 键盘控制说明 (键盘控制窗口需获得焦点)

| 按键 | 功能 |
|------|------|
| `1` | 回到 PASSIVE |
| `2` | FIXEDDOWN (趴下) |
| `3` | FIXEDSTAND → RL 策略接管 |
| `W` `A` `S` `D` | xy 线速度 |
| `J` `L` | yaw 角速度(转向) |
| `Space` | 重置速度 |

---

## 六、RL 控制器配置

### 6.1 参数文件

| 文件 | 用途 |
|------|------|
| `robot_control.yaml` | 控制器定义、关节列表、接口配置 |
| `config/abs/config.yaml` | RL 模型参数、观测空间、PD 增益 |
| `config/abs/policy.pt` | 训练好的 Agile Policy 模型 |

### 6.2 关键参数 (ABS 配置)

```yaml
# robot_control.yaml
rl_quadruped_controller:
  ros__parameters:
    update_rate: 200       # 控制器频率 (Hz)
    model_folder: "abs"    # config/abs/ 目录下的配置和模型
    joints:                # 关节顺序: FL→FR→RL→RR
      - FL_hip_joint
      - FL_thigh_joint
      - FL_calf_joint
      ...

# config/abs/config.yaml
decimation: 4             # RL 推理频率 = 200/4 = 50 Hz
num_observations: 61      # 观测维度
action_scale: 0.25        # 动作缩放
hip_scale_reduction: 0.5  # hip 关节动作减半
rl_kp: [30, 30, ...]      # PD 位置增益
rl_kd: [0.65, ...]        # PD 速度增益
damping: 0.65
stiffness: 30.0
abs_max_episode_length_s: 9.0  # ABS episode 最大长度
abs_contact_threshold: 1.0     # 接触检测阈值
abs_ray2d_count: 11            # 射线数量
abs_ray2d_max_range: 6.0       # 最大射线距离 (m)
```

### 6.3 电机编号

Go2 电机编号 (按 Unitree 官方文档):

| 索引 | 关节 | 说明 |
|------|------|------|
| 0 | FL_hip | 前左髋关节 |
| 1 | FL_thigh | 前左大腿 |
| 2 | FL_calf | 前左小腿 |
| 3 | FR_hip | 前右髋关节 |
| 4 | FR_thigh | 前右大腿 |
| 5 | FR_calf | 前右小腿 |
| 6 | RL_hip | 后左髋关节 |
| 7 | RL_thigh | 后左大腿 |
| 8 | RL_calf | 后左小腿 |
| 9 | RR_hip | 后右髋关节 |
| 10 | RR_thigh | 后右大腿 |
| 11 | RR_calf | 后右小腿 |

---

## 七、地形工具

### 7.1 安装依赖

```bash
pip3 install noise opencv-python numpy
```

### 7.2 使用

```bash
cd ~/quadruped_robots/unitree_mujoco/terrain_tool
# 修改 terrain_generator.py 开头的配置
python3 ./terrain_generator.py
```

### 7.3 地形类型

| 函数 | 效果 |
|------|------|
| `AddBox()` | 立方体障碍 |
| `AddGeometry()` | 几何体 (圆柱/球/椭球等) |
| `AddStairs()` | 楼梯 |
| `AddSuspendStairs()` | 悬浮楼梯 |
| `AddRoughGround()` | 随机方块杂乱地面 |
| `AddPerlinHeighField()` | Perlin 噪声高程图 |
| `AddHeighFieldFromImage()` | 图片 → 高程图地形 |

### 7.4 启用地形

修改 `unitree_mujoco/simulate/config.yaml`:
```yaml
robot_scene: "scene_terrain.xml"
```

---

## 八、Sim-to-Real (真机部署)

### 8.1 关键差异

| 项目 | 仿真 | 真机 |
|------|------|------|
| domain_id | 1 | 0 (默认) |
| 网卡 | lo (回环) | 实际网卡 (如 enp3s0) |
| 硬件接口 | hardware_unitree_mujoco | 同一个插件 |

### 8.2 切换方法

修改 `go2_description/xacro/ros2_control.xacro`:
```xml
<hardware>
    <plugin>hardware_unitree_mujoco/HardwareUnitree</plugin>
    <param name="domain">1</param>          <!-- 仿真=1, 真机=0 -->
    <param name="network_interface">lo</param>  <!-- 仿真=lo, 真机=实际网卡名 -->
</hardware>
```

### 8.3 真机启动前注意事项

1. **必须先关闭机器人自带的运控服务**，否则 DDS 命令会冲突
2. 先在仿真中充分测试
3. 使用 Unitree 遥控器需额外配置 `unitree_joystick_input`

---

## 九、仿真启动校验清单

以下日志表示 RL 策略成功运行:

```
[INFO] [StateRL]: [VERIFY-ENTER] contact shape=1x4
[INFO] [StateRL]: [VERIFY-OBS] computed observation shape: 1x61
[INFO] [StateRL]: [VERIFY-ACTION] model output shape: 1x12
[INFO] [StateRL]: [VERIFY-CMD] FL_hip: q=0.2702 kp=30.00 kd=0.65
[INFO] [StateRL]: [VERIFY-EXIT] RL steps executed: 210
```

关键检查:
- [x] observation shape: **1x61** (61维观测)
- [x] model output: **1x12** (12个关节)
- [x] PD gain: **kp=30.00, kd=0.65**
- [x] RL 正常运行 (step count > 0)
- [ ] **contact 应为非零** (当前 foot force 始终为 0，待修复)

---

## 十、故障排除

### 10.1 libddsc.so.0 not found

```
Error: dlopen error: libddsc.so.0: cannot open shared object file
```

解决:
```bash
export LD_LIBRARY_PATH="/home/lidio/Libraries/unitree_sdk2/lib:$LD_LIBRARY_PATH"
```

### 10.2 机器人不动

**症状**: MuJoCo 窗口显示机器人，但关节不动

**原因**: 没有启动 `unitree_mujoco` 物理仿真进程

**解决**: 先在终端 1 启动 `./simulate/build2/unitree_mujoco -r go2`，再在终端 2 启动 ROS2

### 10.3 机器人瘫倒

**症状**: 进入 RL 后机器人立刻瘫倒

**原因**: `robot_command_` 在 `StateRL::enter()` 中未初始化（已修复）

**解决**: 确保已重新编译最新代码 (`colcon build`)

### 10.4 足端力始终为 0

**症状**: `contact=[-1 -1 -1 -1]`, `forces=[0.0 0.0 0.0 0.0]`

**原因**: 脚力传感器未正确连接 (怀疑)

**影响**: 策略收到的接触信息不准确，但控制指令仍然正常

### 10.5 CycloneDDS 冲突

**症状**: 仿真无法启动，或需要 sudo

**解决**: 卸载 CycloneDDS RMW，使用 FastDDS:
```bash
sudo apt remove ros-humble-rmw-cyclonedds-cpp
sudo apt install ros-humble-rmw-fastrtps-cpp
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
```

### 10.6 libtorch 找不到

```bash
export LD_LIBRARY_PATH="/home/lidio/Libraries/libtorch-cpu-2.0.1/lib:$LD_LIBRARY_PATH"
```

---

## 十一、相关链接

| 项目 | URL |
|------|-----|
| unitree_mujoco | https://github.com/legubiao/unitree_mujoco |
| quadruped_ros2_control | https://github.com/legubiao/quadruped_ros2_control |
| unitree_sdk2 | https://github.com/unitreerobotics/unitree_sdk2 |
| Unitree 文档 | https://support.unitree.com/home/zh/developer |
| MuJoCo 文档 | https://mujoco.readthedocs.io/en/stable/ |
| ABS 论文代码 | https://github.com/LeCAR-Lab/ABS |
