# 日报 2026-06-04

## 今日完成

### D.1 障碍物检测修复 ✅
- **最终方案**: 纯 2D 几何 ray-geom 交叉，匹配训练 `circle_ray_query` 数学逻辑
- 跳过 robot geom (group 2,3) + floor plane，仅对障碍物 geom 做 2D ray-circle 交叉
- `flg_static=1` 方案失败（MuJoCo 3.3.3 ray 总是先碰地面，无法跳过），回退到 z=0.25 方案（射线飞越矮障碍物），最终用 2D 方案解决
- **验证**: scene_terrain.xml 中正确检测到障碍物，ray2d=[2.58 2.58 0.19 -0.14 -0.27 ...]

### D.2 Recovery Twist 优化 ✅
- **移植 ROS1 梯度下降**: 3 次迭代优化 twist，最小化 RA 值
- LibTorch autograd 实现，参数匹配 ROS1 (twist_lam=10, lr=0.5, tau=0.05)
- **首次 auto-switch 成功**: TWIST-OPT twist: vx=-0.188 vy=-0.244 wz=-0.567（后退+转向）
- **自主循环**: RL→auto-switch→Recovery(150步)→自动回RL→重新评估
- Recovery 从之前立刻倒下改善到可持续运行

### 配置化
- 所有 twist 优化参数写入 `config.yaml`，无需重新编译即可调参
- CtrlComponent 共享 RA 模型、recovery 参数、优化 twist

### 启动脚本
- `scripts/launch_abs_sim.sh`：一键启动 MuJoCo + Controller
- 键盘输入需另外手动启动

### CLAUDE.md 规则
- 新增规则 0：遇到问题先看 ROS1 源码 `ABS/deployment/src/abs_src/`

## 问题与解决

1. **mj_ray() 3D 射线永远先碰地面** → 改用 2D 几何 ray-circle 交叉
2. **bodyexclude 只对 weld 约束生效** → 用 geom group 过滤
3. **flg_static=1 不生效** → 放弃 mj_ray，纯 2D 几何
4. **梯度下降 autograd 报错** → `GradMode::set_enabled(true)` 重新打开
5. **Recovery 死循环** → 加冷却期 + auto-return

## 当前瓶颈

Go2 Recovery 策略质量不够，无法稳定执行 twist 优化产生的避障命令。服务器已开始重训 (15000 iters)。

## 明日计划

1. 本地继续调参 (twist_lam=3.0, vy=±0.15)
2. 等待服务器重训完成 → 下载新 recovery 模型
3. 替换 policy.pt 重新测试自主避障
4. 制定 Phase E (真机安全) 详细计划
